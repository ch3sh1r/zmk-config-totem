draw_config:
  # Move all combos to their own mini-diagrams at the bottom
  separate_combo_diagrams: true
  
  # How much smaller the combo mini-diagrams should be (default is 2)
  combo_diagrams_scale: 2
